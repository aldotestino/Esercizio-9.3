<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Corsi</title>
</head>

<body>
  <h1>Corsi</h1>
  <% if(students.length===0){ %>
    <p>Non ci sono corsi disponibili, <a href="/students/add">clicca qui</a> per aggiungere uno studente.</p>
    <% } else{ %>
      <ul>
        <% for(let i=0; i < students.length; i++) {%>
          <li>
            <%= students[i].firstName %>
              <%= students[i].lastName %>;
                id: <%= students[i]._id %>
          </li>
          <% }%>
      </ul>
      <% } %>
</body>

</html>